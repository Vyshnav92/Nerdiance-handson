# Grafana Dashboard Imports

To import these dashboards:
1. Grafana > Dashboards > New > Import
2. Upload `service-health.json` or `cluster-metrics.json` (or paste contents)
3. Select "Prometheus" as the data source when prompted
4. Click Import
