apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: block-handson-app
  namespace: default
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: handson-app
  policyTypes:
    - Ingress
  ingress: []
