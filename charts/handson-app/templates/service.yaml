apiVersion: v1
kind: Service
metadata:
  name: {{ include "handson-app.fullname" . }}
  labels:
    {{- include "handson-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "handson-app.selectorLabels" . | nindent 4 }}
